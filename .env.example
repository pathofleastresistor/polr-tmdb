HA_CONFIG=../ha-config/homeassistant
HA_WWW=../ha-config/homeassistant/www/polr_tmdb
HA_RESOURCES_FILE=../ha-config/homeassistant/.storage/lovelace_resources
# If HA runs in Docker with the project mounted at a different path inside the container,
# set these to the container-internal paths so symlinks resolve correctly inside Docker.
# HA_COMPONENT_LINK_TARGET=/config/tmdb_dev/custom_components/polr_tmdb
# HA_WWW_LINK_TARGET=/config/tmdb_dev/www/polr_tmdb
